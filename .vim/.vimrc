"----Compiling useful settings/configuration----

" relaod files changed outside of Vim not currently modified in Vim
" set autoread

" use Unicode=utf-8
" set encoding=utf-8

" errors flash screen rather than emith beep
" set visualbell

" make Backspace work like Delete
" set backspace=indent,eol,start

" don't create `filename~` backup
" set nobackup

" don't create temp files
" set noswapfile

" line numbers & distances
" set relativenumber
" set number

" number of lines offset when jumping
" set scrolloff=2

"
